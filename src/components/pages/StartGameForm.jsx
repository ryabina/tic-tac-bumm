import React, { useState } from 'react';
import styled from 'styled-components';

const Start = styled.button`
  background-color: black,
  `;

const StartGameForm = () => { return (<Start> Start Game </Start>); };
export default StartGameForm;
